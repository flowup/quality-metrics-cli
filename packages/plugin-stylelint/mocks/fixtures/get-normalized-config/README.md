## Why in the .stylelintrc.ts the lint rule for short arrays is ignored

Stylelint has a specific behaviour of converting `true` to `[true]`, so for testing purposes we are adding the array directly here.
